import numpy as np
import pandas as pd
class autoBagging:

    def __init__(type):
        if type == 'create':
            self.type == type
        if type == 'predict':
            self.type == type
        else:
            self.type == error
    
    def create(FileNameDataset, TargetsNames, Metafunctions, PostProcessing):
        # Por cada file abrir o csv e tirar para um nparray

    def predict(form, data):
        if data[form].ntype != np.string